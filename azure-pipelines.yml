# Node.js
# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- '*'

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'

- task: Npm@1
  inputs:
    command: 'install'
    workingDir: 'CxScan'

- task: TfxInstaller@2
  inputs:
    version: 'v0.6.x'

- task: CmdLine@2
  inputs:
    script: 'npm install'
  displayName: 'npm install'

- task: CmdLine@2
  inputs:
    script: 'echo "IMAGES:"; cd images; ls; cd ..; echo "TFX DIRECTORY:"; ls; tfx extension create --manifest-globs vss-extension.json'
  displayName: 'tfx extension create'

- task: CopyFiles@2
  inputs:
    SourceFolder: '/home/vsts/work/_temp/s/'
    Contents: '*.vsix'
    TargetFolder: '$(Build.ArtifactStagingDirectory)'
  displayName: 'Build Source Directory'

- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)'
    ArtifactName: 'drop'